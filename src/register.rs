extern crate discord;
extern crate chrono;

use discord::model;
use std::str::SplitWhitespace;

pub fn register(_message: &model::Message, _arguments: SplitWhitespace) {
	//make sure proper number of arguments
}